apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: socketio-test-service-2
spec:
  refreshInterval: 1m
  secretStoreRef:
    kind: SecretStore
    name: vault-kv
  target:
    name: socketio-test-service-2
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: socketio-test-service-2
